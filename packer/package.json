{
  "name": "fadc-packer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FAdC R2 backplane decomposition and rack packing. Zero dependencies.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "score": "node scripts/score.ts",
    "extract": "python scripts/extract_reference.py"
  }
}
